{
  "name": "fibonaccifootball",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Fibonacci Football League \u2014 Vercel serverless API + Vercel Neon Postgres",
  "scripts": {
    "dev": "vercel dev",
    "build": "echo 'Vercel builds serverless functions automatically'",
    "start": "vercel dev",
    "test": "node --experimental-vm-modules --test"
  },
  "dependencies": {
    "@vercel/postgres": "^0.9.0"
  },
  "devDependencies": {
    "@electric-sql/pglite": "^0.5.8"
  }
}
